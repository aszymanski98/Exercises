let n;

function liczba (n) {
    if (100 <= n && n <= 200) {
        console.log("Liczba znajduje się w przedziale");
    } else {
        console.log("Liczba nie znajduje się w przedziale");
    }
}

liczba(199);